# Roadmap (working)

## 0.4.x
- Solidify Windows CANable support (SLCAN init presets; read-only)
- Add cantools/canmatrix optional DBC parsing + validation
- Add decoder pipeline: frames.jsonl -> signals.jsonl
- Add “command catalog” file format + signature verification (GM-supervised posture)
- Add artifact pack command: tar/zip session folder + manifest

## 0.5.x
- J2534 enumeration + safe smoke tests (read-only; no security access)
- Basic dashboard exporter (CSV / parquet)
- Expand LNF fault-tree rule library
